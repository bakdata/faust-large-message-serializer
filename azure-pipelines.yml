
trigger:
  branches:
    include:
      - refs/heads/*
      - refs/tags/*
pr:
  branches:
    include:
    - master

variables:
- group: testpypi


resources:
  repositories:
    - repository: templates
      type: github
      name: bakdata/bakdata-project-templates
      endpoint: bot
      ref: feature/python


stages:
  - template: azure/gradle/python-artifact.yml@templates